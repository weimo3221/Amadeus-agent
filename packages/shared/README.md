# Shared

Shared types, event contracts, and config schemas live here.

Keep this package dependency-free where possible so both desktop and server can import it without pulling runtime-specific code.
