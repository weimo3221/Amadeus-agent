# Live2D Stage

Live2D rendering and character stage control.

Initial public API should cover:

- load model
- set expression
- play motion
- set assistant state
- update lipsync
- pointer tracking

Use `../airi/packages/stage-ui-live2d` as the main reference, but keep this package smaller for the first version.
