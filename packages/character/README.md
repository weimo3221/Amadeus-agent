# Character

Persona and behavior mapping.

This package turns agent/runtime state into character-facing behavior:

- persona prompt
- emotion policy
- expression mapping
- motion mapping
- safety and permission style

Config starts in `configs/character.yaml`.
